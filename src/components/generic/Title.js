import styled from 'styled-components';

export default styled.h1`
  font-family: var(--font-brand);
  color: var(--color-light);
  font-size: 5vmax;
  padding: 0;
  margin: auto;
  line-height: 1.25;

  text-shadow: 2px 2px 0 var(--color-primary), 4px 4px 0 var(--color-accent),
    6px 6px 0 var(--color-dark);
`;
